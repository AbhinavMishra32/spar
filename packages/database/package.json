{
  "name": "@pracai/database",
  "version": "0.1.0",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run --passWithNoTests", "lint": "tsc --noEmit", "build": "tsc -p tsconfig.build.json", "generate": "drizzle-kit generate" },
  "dependencies": { "drizzle-orm": "^0.44.3", "postgres": "^3.4.7" },
  "devDependencies": { "drizzle-kit": "^0.31.4", "typescript": "^5.8.3", "vitest": "^3.2.4" }
}

