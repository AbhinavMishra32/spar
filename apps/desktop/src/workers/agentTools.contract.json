{
  "ask_user_question": {
    "description": "Suspend the session for one focused learner answer. Offer 2-3 mutually exclusive choices. Each option is one self-contained line the learner can scan — no subtitle, no second sentence — so write the whole choice into the label. Always allow a custom answer.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "questions": {
          "items": {
            "additionalProperties": false,
            "properties": {
              "custom": {
                "default": true,
                "type": "boolean"
              },
              "header": {
                "maxLength": 40,
                "minLength": 1,
                "type": "string"
              },
              "multiple": {
                "default": false,
                "type": "boolean"
              },
              "options": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "label": {
                      "maxLength": 120,
                      "minLength": 1,
                      "type": "string"
                    }
                  },
                  "required": [
                    "label"
                  ],
                  "type": "object"
                },
                "maxItems": 3,
                "minItems": 2,
                "type": "array"
              },
              "question": {
                "maxLength": 1000,
                "minLength": 3,
                "type": "string"
              }
            },
            "required": [
              "header",
              "question",
              "options"
            ],
            "type": "object"
          },
          "maxItems": 3,
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "questions",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "assign_practice_problem": {
    "description": "Set a real problem from any available provider as this session's challenge. Use the exact `source` and `slug` returned by search. Prefer this over create_question whenever a real problem genuinely lands on the target you chose: it carries the provider's judge, calibrated difficulty, and the learner's history when that provider is connected. Read it first, and check its rating against the context's learnerStanding.setProblemsRated — the host refuses a problem priced outside that range, and says what the range was. The host mounts it, tags it with the concepts you name, and returns the challenge; do not describe its contents in your reply because the learner is about to read it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "What this challenge is about, in Spar's vocabulary, most specific first. Exactly one entry has role primary and it must name the gap the target describes — not merely the topic the source files the problem under.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "kind": {
                "enum": [
                  "dsa",
                  "engineering",
                  "craft"
                ],
                "type": "string"
              },
              "parentSlug": {
                "description": "the area a new sub-concept belongs under, e.g. sliding-window",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "role": {
                "default": "supporting",
                "enum": [
                  "primary",
                  "supporting"
                ],
                "type": "string"
              },
              "slug": {
                "description": "kebab-case, as specific as the challenge really is: prefer window-invariant-restoration over sliding-window, or aliasing over references-and-mutation",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "title": {
                "description": "only needed for a concept Spar has not seen before",
                "maxLength": 80,
                "minLength": 2,
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "maxItems": 5,
          "minItems": 1,
          "type": "array"
        },
        "language": {
          "description": "The language to write this challenge in. Omit only when the context's preferredLanguage is already right; name one whenever the learner has asked for a different language in this session, because that is what makes their request stick beyond this turn.",
          "enum": [
            "javascript",
            "typescript",
            "python",
            "java",
            "c",
            "cpp",
            "go",
            "rust",
            "swift",
            "ruby"
          ],
          "type": "string"
        },
        "replaceReason": {
          "description": "Required only when a challenge is already open and this problem is to take its place — say what the learner asked for. Their attempt is closed as replaced and this problem records it as its predecessor. Never set it to move someone off a challenge they did not ask to leave.",
          "maxLength": 500,
          "minLength": 3,
          "type": "string"
        },
        "slug": {
          "description": "The problem's URL slug, exactly as the source gave it.",
          "maxLength": 120,
          "minLength": 1,
          "type": "string"
        },
        "source": {
          "description": "The provider identity returned by search. A slug is only unique inside its provider.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        },
        "why": {
          "description": "One or two sentences: why this specific problem discriminates what is still uncertain about this learner. This is stored with the challenge and is what a later turn reads to know what you were testing.",
          "maxLength": 400,
          "minLength": 20,
          "type": "string"
        }
      },
      "required": [
        "source",
        "slug",
        "concepts",
        "why",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "commit_session_decision": {
    "description": "Commit exactly one next pedagogical action.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "action": {
          "enum": [
            "diagnose",
            "teach",
            "practise",
            "transfer",
            "advance",
            "retain"
          ],
          "type": "string"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "action",
        "reason",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "create_question": {
    "description": "Compile and validate a complete question from the active target. All paths are relative and the reference implementation must replace starter implementation files.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "accidentalDifficulty": {
          "items": {
            "type": "string"
          },
          "maxItems": 3,
          "type": "array"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "What this challenge is about, most specific first. Exactly one entry has role primary: the concept the challenge is aimed at.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "kind": {
                "enum": [
                  "dsa",
                  "engineering",
                  "craft"
                ],
                "type": "string"
              },
              "parentSlug": {
                "description": "the area a new sub-concept belongs under, e.g. sliding-window",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "role": {
                "default": "supporting",
                "enum": [
                  "primary",
                  "supporting"
                ],
                "type": "string"
              },
              "slug": {
                "description": "kebab-case, as specific as the challenge really is: prefer window-invariant-restoration over sliding-window, or aliasing over references-and-mutation",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "title": {
                "description": "only needed for a concept Spar has not seen before",
                "maxLength": 80,
                "minLength": 2,
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "maxItems": 5,
          "minItems": 1,
          "type": "array"
        },
        "difficulty": {
          "description": "How hard to write it, as an absolute price rather than a feeling: foundation is rated 900, developing 1200, proficient 1500, advanced 1800, on the same scale as the learner's own rating. Write to context.learnerStanding.writeProblemsAt, which is the word that lands in the window this ability's status calls for — the host checks this the same way it checks a problem you fetch, and rejects a challenge pitched outside it. A challenge they would almost certainly solve teaches nothing when they solve it.",
          "enum": [
            "foundation",
            "developing",
            "proficient",
            "advanced"
          ],
          "type": "string"
        },
        "expectedFailureSignatures": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        },
        "hiddenTests": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "The grader. It must run at least twenty-four cases, which means a generated sweep and not a longer list typed out: loop over inputs from a seeded pseudo-random generator, compute each expected answer with a brute-force oracle written inside the test file itself, and emit one verdict line per case whose name contains the actual input. Keep your targeted cases for the known misconceptions alongside it. On failure every case must print its input, expected and actual — the learner cannot see this file, so a failure that does not say what it ran leaves them guessing.",
          "type": "object"
        },
        "kind": {
          "enum": [
            "function",
            "module",
            "repair",
            "extension",
            "repository"
          ],
          "type": "string"
        },
        "knownIncorrectFiles": {
          "items": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object"
          },
          "minItems": 1,
          "type": "array"
        },
        "language": {
          "enum": [
            "javascript",
            "typescript",
            "python",
            "java",
            "c",
            "cpp",
            "go",
            "rust",
            "swift",
            "ruby"
          ],
          "type": "string"
        },
        "referenceFiles": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "requiresComplexityAnalysis": {
          "description": "Choose true only when asymptotic time and auxiliary-space reasoning is part of what this challenge trains: algorithms, data structures, or scaling-sensitive implementation. Choose false for syntax, API use, debugging, UI, refactoring, repository mechanics, and semantic behavior where Big-O adds no useful evidence.",
          "type": "boolean"
        },
        "runCommand": {
          "minLength": 1,
          "type": "string"
        },
        "solutionRequirements": {
          "description": "How the solution must be written, when the point of the challenge depends on it: \"linear time, one pass\", \"recursive\", \"no built-in sort\". Shown to the learner with the problem, and checked by review_solution after the tests pass — a solution that passes but ignores these is sent back. State a requirement only when a different approach would defeat the exercise; a challenge with nothing to insist on omits this entirely.",
          "items": {
            "maxLength": 160,
            "minLength": 4,
            "type": "string"
          },
          "maxItems": 4,
          "type": "array"
        },
        "starterFiles": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "statement": {
          "description": "The complete learner-facing problem page, in this order and nothing else: one paragraph saying what to implement; one line per rule it must satisfy; then `Examples`, and under it each example as `Input:` / `Output:` / `Explanation:` lines, the explanation being why that answer is the answer; then `Constraints`. Do not write a heading over the description or number the examples yourself — the app draws that hierarchy and your own headings and numbering appear on top of it. No agent commentary, selection rationale, validation notes, or hidden-test details.",
          "minLength": 30,
          "type": "string"
        },
        "title": {
          "description": "A concise, professional problem title naming the operation or result; never an agent action, lesson, file, or bug category.",
          "minLength": 3,
          "type": "string"
        },
        "visibleTests": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "The contract the learner reads. At least four cases, each named and written by hand: the ordinary one, each boundary, and the one that separates the right idea from the plausible wrong one. No generated sweep here.",
          "type": "object"
        }
      },
      "required": [
        "concepts",
        "title",
        "language",
        "kind",
        "difficulty",
        "requiresComplexityAnalysis",
        "statement",
        "starterFiles",
        "referenceFiles",
        "visibleTests",
        "hiddenTests",
        "knownIncorrectFiles",
        "runCommand",
        "accidentalDifficulty",
        "expectedFailureSignatures",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "open_visualizer": {
    "description": "Load the execution visualiser: Spar can run code under a real tracer and draw every variable, structure and pointer at any step, inline in this conversation. Call this whenever the learner is confused about what their code is actually doing at some point in the run — a value that is not what they expect, a loop that ends early, a pointer somewhere surprising, an off-by-one — or when a small idea would land better shown than described. Returns instructions and the visualiser's tools.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        }
      },
      "required": [
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "propose_ability_update": {
    "description": "Propose a versioned markdown ability change backed by evidence. Include summary, concepts and practice whenever the evidence now supports naming this as something the learner can do.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "abilityId": {
          "format": "uuid",
          "type": "string"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "The concepts this ability covers, which is how the learner reaches the challenges behind it.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "kind": {
                "enum": [
                  "dsa",
                  "engineering",
                  "craft"
                ],
                "type": "string"
              },
              "parentSlug": {
                "description": "the area a new sub-concept belongs under, e.g. sliding-window",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "role": {
                "default": "supporting",
                "enum": [
                  "primary",
                  "supporting"
                ],
                "type": "string"
              },
              "slug": {
                "description": "kebab-case, as specific as the challenge really is: prefer window-invariant-restoration over sliding-window, or aliasing over references-and-mutation",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "title": {
                "description": "only needed for a concept Spar has not seen before",
                "maxLength": 80,
                "minLength": 2,
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "maxItems": 6,
          "type": "array"
        },
        "evidence": {
          "description": "Nuanced interpretations of exact durable events, at least one. Describe the behavior observed, not a score: which step of the idea held and which did not, and whether it held again on the second occasion. \"Failed the window question\" is not an interpretation of anything.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "eventId": {
                "format": "uuid",
                "type": "string"
              },
              "independence": {
                "enum": [
                  "independent",
                  "assisted",
                  "unknown"
                ],
                "type": "string"
              },
              "polarity": {
                "enum": [
                  "supporting",
                  "contradictory",
                  "neutral"
                ],
                "type": "string"
              },
              "statement": {
                "maxLength": 300,
                "minLength": 8,
                "type": "string"
              },
              "strength": {
                "maximum": 1,
                "minimum": 0,
                "type": "number"
              }
            },
            "required": [
              "eventId",
              "statement",
              "polarity",
              "independence",
              "strength"
            ],
            "type": "object"
          },
          "maxItems": 8,
          "minItems": 1,
          "type": "array"
        },
        "evidenceEventIds": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "markdown": {
          "type": "string"
        },
        "pattern": {
          "additionalProperties": false,
          "description": "A mistake lifecycle update. The host will refuse to promote a pattern unless evidence links span at least two attempts.",
          "properties": {
            "description": {
              "maxLength": 400,
              "minLength": 10,
              "type": "string"
            },
            "evidenceEventIds": {
              "items": {
                "format": "uuid",
                "type": "string"
              },
              "maxItems": 12,
              "type": "array"
            },
            "status": {
              "enum": [
                "observation",
                "hypothesis",
                "pattern",
                "monitoring",
                "resolved"
              ],
              "type": "string"
            },
            "title": {
              "maxLength": 100,
              "minLength": 3,
              "type": "string"
            }
          },
          "required": [
            "title",
            "description",
            "status",
            "evidenceEventIds"
          ],
          "type": "object"
        },
        "practice": {
          "description": "Drills for going further on this exact ability, each phrased as the learner's own goal because each one starts a session. Vary the transfer context, not just the difficulty.",
          "items": {
            "maxLength": 180,
            "minLength": 10,
            "type": "string"
          },
          "maxItems": 4,
          "type": "array"
        },
        "status": {
          "description": "Omit to let the evidence count decide. Set it only to say something the count cannot, such as marking a long-untouched ability stale.",
          "enum": [
            "uncertain",
            "developing",
            "independent",
            "stale"
          ],
          "type": "string"
        },
        "summary": {
          "description": "One sentence in plain words: what the learner can now do. Written to them, not about them.",
          "maxLength": 220,
          "minLength": 10,
          "type": "string"
        }
      },
      "required": [
        "abilityId",
        "markdown",
        "evidenceEventIds",
        "evidence",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_ability": {
    "description": "Read one versioned ability document, with the mistake patterns filed under it and its most recent behavioural evidence. The markdown is the claim; the patterns are what is still open about it. Read both before proposing an update — an update written from the document alone can only restate it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "abilityId": {
          "format": "uuid",
          "type": "string"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        }
      },
      "required": [
        "abilityId",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_attempt": {
    "description": "Read an attempt's current code, deterministic runner verdict, and recorded solve history. The runner is the authority on correctness. Returns the complete report and source files, plus a sequence-to-event-ID index for ability evidence. Payloads are represented once in the report instead of duplicated as raw events. Start with one call and work from that evidence. Use sections, eventTypes, cases or scope when you want a focused view.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "attemptId": {
          "type": "string",
          "format": "uuid",
          "description": "Omit for the attempt the learner has open right now, which is almost always the one you mean. Name one only to read a different attempt out of their history."
        },
        "sections": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "log",
              "cases",
              "runs",
              "timings"
            ]
          },
          "minItems": 1,
          "maxItems": 4,
          "description": "log: every event, in order, with its payload and per-case lines. cases: each case's verdict in every run, with pass and failure counts. runs: each run's score and which cases newly passed or newly failed against the last run that saw them. timings: totals, the gap before the first run, the longest gap between events, and each edit stretch. Defaults to log, cases and runs."
        },
        "eventTypes": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 3,
            "maxLength": 40
          },
          "maxItems": 12,
          "description": "Keep only these event types in the log, e.g. [\"test_run\"] for nothing but the runs, or [\"file_changed\",\"test_run\"] for the edit-and-run rhythm. Omit for every type. Recorded types: attempt_started, file_changed, command_executed, test_run, submission_created, submission_evaluated, attempt_completed, hint_requested, learner_remark, agent_message."
        },
        "cases": {
          "type": "string",
          "enum": [
            "all",
            "failed-ever",
            "still-failing",
            "fixed"
          ],
          "description": "Narrow the case history. `still-failing` is what is wrong now; `fixed` is what they repaired themselves, which is evidence of learning inside one attempt."
        },
        "scope": {
          "type": "string",
          "enum": [
            "all",
            "since-last-submission"
          ],
          "description": "`since-last-submission` keeps only what happened after the last graded run, which is often the whole question on a follow-up turn."
        },
        "caseDetail": {
          "type": "string",
          "enum": [
            "brief",
            "full"
          ],
          "description": "`brief` drops the expected/actual pair from each failing case line in the log. Default full."
        },
        "maxLines": {
          "type": "integer",
          "minimum": 20,
          "maximum": 2000,
          "description": "Cap on log lines, newest kept, and it says how many it dropped. Omit to return all log lines. Only applies when explicitly requested."
        },
        "actionTitle": {
          "type": "string",
          "minLength": 3,
          "maxLength": 70,
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs."
        }
      },
      "required": [
        "actionTitle"
      ],
      "additionalProperties": false,
      "$schema": "http://json-schema.org/draft-07/schema#"
    }
  },
  "read_challenge": {
    "description": "Read one stored challenge design, validation report, attempts, and test history.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "questionId": {
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "questionId",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_concept_graph": {
    "description": "Read the concept vocabulary near a topic, with this learner's evidence against each one. Use it to choose what to tag and what to test next.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 14,
          "maximum": 24,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_daily_practice_problem": {
    "description": "The source's problem of the day, in full. Worth offering when the learner has no particular direction, and worth ignoring when they do — it is chosen for everybody, so it is unlikely to land on their gap.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "source": {
          "description": "The provider identity returned by search. Keep it paired with the slug.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        }
      },
      "required": [
        "source",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_lesson": {
    "description": "Read one lesson you have already taught: its pages, its takeaways and its reading list. Use it before teaching near something in your context's recentLessons, so the new lesson continues that one instead of repeating it, and before claiming you taught something specific, so what you say you showed them is what you showed them.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "lessonId": {
          "type": "string",
          "format": "uuid"
        },
        "actionTitle": {
          "type": "string",
          "minLength": 3,
          "maxLength": 70,
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs."
        }
      },
      "required": [
        "lessonId",
        "actionTitle"
      ],
      "additionalProperties": false,
      "$schema": "http://json-schema.org/draft-07/schema#"
    }
  },
  "read_practice_problem": {
    "description": "Read one problem in full: its statement, its signature, the languages it publishes starters for, the cases Spar can run locally, the concepts it maps onto, and the problems the source says it is related to. Read this before assigning a problem — the statement is the only way to know whether it actually exercises the gap you are aiming at, and the reply says plainly who will grade it.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "includeStatement": {
          "default": true,
          "description": "Set false when you only need the shape and the tags, which is most of a fit decision.",
          "type": "boolean"
        },
        "slug": {
          "description": "The problem's URL slug, e.g. \"two-sum\". Never its number or its title.",
          "maxLength": 120,
          "minLength": 1,
          "type": "string"
        },
        "source": {
          "description": "The provider identity returned by search. Keep it paired with the slug.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        }
      },
      "required": [
        "source",
        "slug",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_practice_progress": {
    "description": "Problems this learner has already touched at the source, newest first. This is the one thing the source knows that Spar's own record cannot: what they attempted elsewhere and never finished. An unfinished attempt is a strong candidate for a target — it is a gap they have already met and walked away from.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 20,
          "maximum": 50,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "maximum": 2000,
          "minimum": 0,
          "type": "integer"
        },
        "source": {
          "description": "The provider identity returned by search. Keep it paired with the slug.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        },
        "status": {
          "default": "attempted",
          "enum": [
            "attempted",
            "solved"
          ],
          "type": "string"
        }
      },
      "required": [
        "source",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_practice_source": {
    "description": "What the connected practice source can do right now, and who it thinks the learner is: the account, how many problems they have solved at each difficulty, and the source's own per-topic solve counts. Read the solve counts as weak prior evidence beside Spar's own ledger — they say what the learner has been exposed to, never how well they understood it. Call this before relying on anything else here, because every learner-specific answer is empty while the source is not connected.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "source": {
          "description": "The provider identity returned by search. Keep it paired with the slug.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        }
      },
      "required": [
        "source",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_practice_submissions": {
    "description": "This learner's own past submissions for one problem, and optionally the code of one of them. An accepted solution they wrote themselves is the strongest possible statement about what they can do, and a rejected one shows exactly where they stalled. Use it when their history with a specific problem is what you are reasoning about.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "includeCode": {
          "default": false,
          "description": "Fetches the source of the most recent submission. Costs an extra request, so ask for it only when the code itself is what you need.",
          "type": "boolean"
        },
        "limit": {
          "default": 5,
          "maximum": 20,
          "minimum": 1,
          "type": "integer"
        },
        "slug": {
          "description": "The problem's URL slug, e.g. \"two-sum\". Never its number or its title.",
          "maxLength": 120,
          "minLength": 1,
          "type": "string"
        },
        "source": {
          "description": "The provider identity returned by search. Keep it paired with the slug.",
          "enum": [
            "leetcode",
            "codeforces"
          ],
          "type": "string"
        }
      },
      "required": [
        "source",
        "slug",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_session": {
    "description": "Read the current session summary and decisions.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "sessionId": {
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "sessionId",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "read_submissions": {
    "description": "Read what the learner actually submitted at a challenge, in order. Without `submissionId` it returns every submission's verdict and case counts — this is the tool for \"what have they tried\", for seeing whether they converged or thrashed, and for finding which submission a question is about. With `submissionId` it returns that one in full: the exact code that was sent and every case it was graded on, including the inputs and expected/actual values of the ones that failed. Cite any submission you refer to as [[submission:<id>|a few words]] so the learner can open it — never quote its id in your prose.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "challengeId": {
          "type": "string",
          "format": "uuid",
          "description": "The challenge whose submissions to list. Omit for the one the learner has open right now."
        },
        "submissionId": {
          "type": "string",
          "format": "uuid",
          "description": "One submission, in full, with its code and cases. Take the id from a previous listing."
        },
        "outcome": {
          "type": "string",
          "enum": [
            "all",
            "passed",
            "failed"
          ],
          "description": "Narrow the listing. `failed` is the record of what they tried and why it was wrong, which is usually the interesting half."
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 40,
          "default": 20,
          "description": "How many submissions to return. The list reads oldest first and a longer history is cut from the front, so the cap keeps the most recent and the ordinals stay absolute."
        },
        "actionTitle": {
          "type": "string",
          "minLength": 3,
          "maxLength": 70,
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs."
        }
      },
      "required": [
        "actionTitle"
      ],
      "additionalProperties": false,
      "$schema": "http://json-schema.org/draft-07/schema#"
    }
  },
  "replace_current_question": {
    "description": "Compile a validated replacement for the active challenge while preserving its attempt, tests, and replacement lineage in history.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "accidentalDifficulty": {
          "items": {
            "type": "string"
          },
          "maxItems": 3,
          "type": "array"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "What this challenge is about, most specific first. Exactly one entry has role primary: the concept the challenge is aimed at.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "kind": {
                "enum": [
                  "dsa",
                  "engineering",
                  "craft"
                ],
                "type": "string"
              },
              "parentSlug": {
                "description": "the area a new sub-concept belongs under, e.g. sliding-window",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "role": {
                "default": "supporting",
                "enum": [
                  "primary",
                  "supporting"
                ],
                "type": "string"
              },
              "slug": {
                "description": "kebab-case, as specific as the challenge really is: prefer window-invariant-restoration over sliding-window, or aliasing over references-and-mutation",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "title": {
                "description": "only needed for a concept Spar has not seen before",
                "maxLength": 80,
                "minLength": 2,
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "maxItems": 5,
          "minItems": 1,
          "type": "array"
        },
        "difficulty": {
          "description": "How hard to write it, as an absolute price rather than a feeling: foundation is rated 900, developing 1200, proficient 1500, advanced 1800, on the same scale as the learner's own rating. Write to context.learnerStanding.writeProblemsAt, which is the word that lands in the window this ability's status calls for — the host checks this the same way it checks a problem you fetch, and rejects a challenge pitched outside it. A challenge they would almost certainly solve teaches nothing when they solve it.",
          "enum": [
            "foundation",
            "developing",
            "proficient",
            "advanced"
          ],
          "type": "string"
        },
        "expectedFailureSignatures": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        },
        "hiddenTests": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "The grader. It must run at least twenty-four cases, which means a generated sweep and not a longer list typed out: loop over inputs from a seeded pseudo-random generator, compute each expected answer with a brute-force oracle written inside the test file itself, and emit one verdict line per case whose name contains the actual input. Keep your targeted cases for the known misconceptions alongside it. On failure every case must print its input, expected and actual — the learner cannot see this file, so a failure that does not say what it ran leaves them guessing.",
          "type": "object"
        },
        "kind": {
          "enum": [
            "function",
            "module",
            "repair",
            "extension",
            "repository"
          ],
          "type": "string"
        },
        "knownIncorrectFiles": {
          "items": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object"
          },
          "minItems": 1,
          "type": "array"
        },
        "language": {
          "enum": [
            "javascript",
            "typescript",
            "python",
            "java",
            "c",
            "cpp",
            "go",
            "rust",
            "swift",
            "ruby"
          ],
          "type": "string"
        },
        "reason": {
          "maxLength": 500,
          "minLength": 3,
          "type": "string"
        },
        "referenceFiles": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "requiresComplexityAnalysis": {
          "description": "Choose true only when asymptotic time and auxiliary-space reasoning is part of what this challenge trains: algorithms, data structures, or scaling-sensitive implementation. Choose false for syntax, API use, debugging, UI, refactoring, repository mechanics, and semantic behavior where Big-O adds no useful evidence.",
          "type": "boolean"
        },
        "runCommand": {
          "minLength": 1,
          "type": "string"
        },
        "solutionRequirements": {
          "description": "How the solution must be written, when the point of the challenge depends on it: \"linear time, one pass\", \"recursive\", \"no built-in sort\". Shown to the learner with the problem, and checked by review_solution after the tests pass — a solution that passes but ignores these is sent back. State a requirement only when a different approach would defeat the exercise; a challenge with nothing to insist on omits this entirely.",
          "items": {
            "maxLength": 160,
            "minLength": 4,
            "type": "string"
          },
          "maxItems": 4,
          "type": "array"
        },
        "starterFiles": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "statement": {
          "description": "The complete learner-facing problem page, in this order and nothing else: one paragraph saying what to implement; one line per rule it must satisfy; then `Examples`, and under it each example as `Input:` / `Output:` / `Explanation:` lines, the explanation being why that answer is the answer; then `Constraints`. Do not write a heading over the description or number the examples yourself — the app draws that hierarchy and your own headings and numbering appear on top of it. No agent commentary, selection rationale, validation notes, or hidden-test details.",
          "minLength": 30,
          "type": "string"
        },
        "title": {
          "description": "A concise, professional problem title naming the operation or result; never an agent action, lesson, file, or bug category.",
          "minLength": 3,
          "type": "string"
        },
        "visibleTests": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "The contract the learner reads. At least four cases, each named and written by hand: the ordinary one, each boundary, and the one that separates the right idea from the plausible wrong one. No generated sweep here.",
          "type": "object"
        }
      },
      "required": [
        "concepts",
        "title",
        "language",
        "kind",
        "difficulty",
        "requiresComplexityAnalysis",
        "statement",
        "starterFiles",
        "referenceFiles",
        "visibleTests",
        "hiddenTests",
        "knownIncorrectFiles",
        "runCommand",
        "accidentalDifficulty",
        "expectedFailureSignatures",
        "reason",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "review_solution": {
    "description": "Judge HOW the passing solution was written, after the runner has already settled that it works. Send it back only for a requirement the challenge actually stated, or for a solution that defeats the point of the exercise — hardcoding the test inputs, calling a library that does the whole thing being taught, or a complexity class the challenge ruled out. A different-but-legitimate approach is not grounds for rework.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "approach": {
          "description": "What they actually did, in one sentence, in their own terms.",
          "maxLength": 300,
          "minLength": 10,
          "type": "string"
        },
        "attemptId": {
          "format": "uuid",
          "type": "string"
        },
        "observedComplexity": {
          "description": "The time complexity of what they actually wrote, e.g. \"O(n)\" or \"O(n log n) from the sort\".",
          "maxLength": 80,
          "minLength": 2,
          "type": "string"
        },
        "reasons": {
          "description": "For `rework`, the requirement missed and what to change — no solution, still only the nudge. For `accepted`, what made it a good use of the idea. Written to the learner.",
          "items": {
            "maxLength": 220,
            "minLength": 8,
            "type": "string"
          },
          "maxItems": 4,
          "type": "array"
        },
        "verdict": {
          "description": "`rework` reopens the challenge for the learner and ends your turn here — no ability update and no next challenge. Use it only when you can name the stated requirement it misses.",
          "enum": [
            "accepted",
            "rework"
          ],
          "type": "string"
        }
      },
      "required": [
        "attemptId",
        "verdict",
        "observedComplexity",
        "approach",
        "reasons",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "search_attempt_history": {
    "description": "Search attempts by ability or failure signature.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 5,
          "maximum": 10,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "search_challenge_history": {
    "description": "Search the learner's durable challenge library, including outcomes and replacement lineage.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 6,
          "maximum": 12,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "search_concept_evidence": {
    "description": "Read how this learner behaves under one concept, broken down by sub-concept, with the recent challenges and outcomes behind it. This is the tool for finding which specific sub-concept is failing inside an area that looks fine on average.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concept": {
          "description": "a concept slug or a topic in words",
          "minLength": 2,
          "type": "string"
        },
        "limit": {
          "default": 3,
          "maximum": 6,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "concept",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "search_learner_model": {
    "description": "Search this learner's memory at three resolutions at once: the ability documents (`passages`, the standing claims), the mistake patterns open under them, and the individual behavioural observations recorded from past attempts. The last two are your own earlier readings of this person — find the hypothesis you wrote before, and one matching observation now promotes it to a pattern instead of being written down a second time.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 4,
          "maximum": 8,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "search_lessons": {
    "description": "Search what Spar has already taught this learner, by topic or concept. Cite what comes back with [[lesson:id|title]] rather than explaining it again.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "minLength": 2
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12,
          "default": 6
        },
        "actionTitle": {
          "type": "string",
          "minLength": 3,
          "maxLength": 70,
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs."
        }
      },
      "required": [
        "query",
        "actionTitle"
      ],
      "additionalProperties": false,
      "$schema": "http://json-schema.org/draft-07/schema#"
    }
  },
  "search_practice_problems": {
    "description": "Search every available problem provider for real problems. Results carry `source`; keep it paired with `slug` when reading or assigning a candidate. Prefer a fitting real problem over writing one because it carries a real judge, calibrated difficulty, and—when that provider is connected—the learner's own history. Search by Spar concept slug (translated separately to each provider's tags) and narrow by difficulty. `todo` is useful when you want something new.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "Spar concept slugs, e.g. [\"window-invariant-restoration\"]. Translated to the source's own tags, walking up to the area when the sub-concept has no tag of its own.",
          "items": {
            "maxLength": 60,
            "minLength": 2,
            "type": "string"
          },
          "maxItems": 5,
          "type": "array"
        },
        "difficulty": {
          "enum": [
            "easy",
            "medium",
            "hard"
          ],
          "type": "string"
        },
        "limit": {
          "default": 8,
          "maximum": 25,
          "minimum": 1,
          "type": "integer"
        },
        "offset": {
          "default": 0,
          "maximum": 2000,
          "minimum": 0,
          "type": "integer"
        },
        "query": {
          "description": "Free text, for when the concept vocabulary does not reach what you mean.",
          "maxLength": 200,
          "type": "string"
        },
        "status": {
          "default": "todo",
          "description": "`todo` for something they have not tried, `attempted` to return to something they left unfinished, `solved` to look at how they did it before.",
          "enum": [
            "any",
            "todo",
            "attempted",
            "solved"
          ],
          "type": "string"
        }
      },
      "required": [
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "set_session_objective": {
    "description": "Persist a lightweight session objective.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "objective": {
          "type": "string"
        }
      },
      "required": [
        "objective",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "set_training_target": {
    "description": "Persist one primary evidence target.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "ability": {
          "type": "string"
        },
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "avoidTesting": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "desiredEvidence": {
          "type": "string"
        },
        "specificGap": {
          "type": "string"
        }
      },
      "required": [
        "ability",
        "specificGap",
        "desiredEvidence",
        "avoidTesting",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "teach_lesson": {
    "description": "Write the learner a lesson and put it in this conversation. Use it when the useful thing to hand them is an idea rather than a problem — the concept their attempt showed they are missing, a topic they asked about, the thing the next challenge is going to need — and prefer it over explaining at length in your reply, because a reply is gone by the next turn and this is not. Scale it to what is actually being taught: one or two pages for a single misconception, more only when the idea genuinely has that many parts, never pages that exist to round the number up. Each page is one thing, in their language, short enough to hold in mind at once; the body is markdown, may use fenced code, and may reference a concept as [[concept:slug|words]] or an earlier lesson as [[lesson:id|words]] exactly as a reply can. Every entry in `references` says why it is worth their time: use `url` only for a page you actually fetched this turn or know to exist, `reading` for a book or chapter you are naming from memory, and `concept`/`lesson` for Spar's own records. Tag `concepts` in the same vocabulary you tag challenges with, primary first — that is what files this beside what you have tested. The learner can see the lesson, so your reply must not restate its pages: say in a sentence what it is for and what to do with it, and point at it as [[lesson:<the id this returns>|its title]].",
    "inputSchema": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 4,
          "maxLength": 90
        },
        "summary": {
          "type": "string",
          "minLength": 20,
          "maxLength": 300
        },
        "concepts": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 2,
            "maxLength": 60
          },
          "minItems": 1,
          "maxItems": 5,
          "description": "concept slugs, primary first, in the same vocabulary challenges are tagged with"
        },
        "pages": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": {
                "type": "string",
                "minLength": 3,
                "maxLength": 90
              },
              "body": {
                "type": "string",
                "minLength": 20,
                "maxLength": 4000
              },
              "takeaway": {
                "type": "string",
                "minLength": 8,
                "maxLength": 200
              }
            },
            "required": [
              "title",
              "body"
            ],
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 8
        },
        "references": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "kind": {
                    "type": "string",
                    "const": "url"
                  },
                  "label": {
                    "type": "string",
                    "minLength": 3,
                    "maxLength": 120
                  },
                  "url": {
                    "type": "string",
                    "format": "uri"
                  },
                  "note": {
                    "type": "string",
                    "minLength": 8,
                    "maxLength": 240
                  }
                },
                "required": [
                  "kind",
                  "label",
                  "url",
                  "note"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "kind": {
                    "type": "string",
                    "const": "concept"
                  },
                  "label": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 120
                  },
                  "slug": {
                    "type": "string",
                    "minLength": 2,
                    "maxLength": 60
                  },
                  "note": {
                    "type": "string",
                    "minLength": 8,
                    "maxLength": 240
                  }
                },
                "required": [
                  "kind",
                  "label",
                  "slug",
                  "note"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "kind": {
                    "type": "string",
                    "const": "lesson"
                  },
                  "label": {
                    "type": "string",
                    "minLength": 3,
                    "maxLength": 120
                  },
                  "lessonId": {
                    "type": "string",
                    "format": "uuid"
                  },
                  "note": {
                    "type": "string",
                    "minLength": 8,
                    "maxLength": 240
                  }
                },
                "required": [
                  "kind",
                  "label",
                  "lessonId",
                  "note"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "kind": {
                    "type": "string",
                    "const": "reading"
                  },
                  "label": {
                    "type": "string",
                    "minLength": 3,
                    "maxLength": 120
                  },
                  "note": {
                    "type": "string",
                    "minLength": 8,
                    "maxLength": 240
                  }
                },
                "required": [
                  "kind",
                  "label",
                  "note"
                ],
                "additionalProperties": false
              }
            ]
          },
          "maxItems": 6,
          "default": []
        },
        "actionTitle": {
          "type": "string",
          "minLength": 3,
          "maxLength": 70,
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs."
        }
      },
      "required": [
        "title",
        "summary",
        "concepts",
        "pages",
        "actionTitle"
      ],
      "additionalProperties": false,
      "$schema": "http://json-schema.org/draft-07/schema#"
    }
  },
  "upsert_ability": {
    "description": "Introduce an uncertain ability, or grant one: append an evidence-backed version and give it the summary, concepts and practice drills that make it something the learner can see and train.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "concepts": {
          "description": "The concepts this ability covers, which is how the learner reaches the challenges behind it.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "kind": {
                "enum": [
                  "dsa",
                  "engineering",
                  "craft"
                ],
                "type": "string"
              },
              "parentSlug": {
                "description": "the area a new sub-concept belongs under, e.g. sliding-window",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "role": {
                "default": "supporting",
                "enum": [
                  "primary",
                  "supporting"
                ],
                "type": "string"
              },
              "slug": {
                "description": "kebab-case, as specific as the challenge really is: prefer window-invariant-restoration over sliding-window, or aliasing over references-and-mutation",
                "maxLength": 60,
                "minLength": 2,
                "type": "string"
              },
              "title": {
                "description": "only needed for a concept Spar has not seen before",
                "maxLength": 80,
                "minLength": 2,
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "maxItems": 6,
          "type": "array"
        },
        "evidence": {
          "description": "Nuanced interpretations of exact durable events. Describe the behavior observed, not a score.",
          "items": {
            "additionalProperties": false,
            "properties": {
              "eventId": {
                "format": "uuid",
                "type": "string"
              },
              "independence": {
                "enum": [
                  "independent",
                  "assisted",
                  "unknown"
                ],
                "type": "string"
              },
              "polarity": {
                "enum": [
                  "supporting",
                  "contradictory",
                  "neutral"
                ],
                "type": "string"
              },
              "statement": {
                "maxLength": 300,
                "minLength": 8,
                "type": "string"
              },
              "strength": {
                "maximum": 1,
                "minimum": 0,
                "type": "number"
              }
            },
            "required": [
              "eventId",
              "statement",
              "polarity",
              "independence",
              "strength"
            ],
            "type": "object"
          },
          "maxItems": 8,
          "type": "array"
        },
        "evidenceEventIds": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "markdown": {
          "minLength": 20,
          "type": "string"
        },
        "pattern": {
          "additionalProperties": false,
          "description": "A mistake lifecycle update. The host will refuse to promote a pattern unless evidence links span at least two attempts.",
          "properties": {
            "description": {
              "maxLength": 400,
              "minLength": 10,
              "type": "string"
            },
            "evidenceEventIds": {
              "items": {
                "format": "uuid",
                "type": "string"
              },
              "maxItems": 12,
              "type": "array"
            },
            "status": {
              "enum": [
                "observation",
                "hypothesis",
                "pattern",
                "monitoring",
                "resolved"
              ],
              "type": "string"
            },
            "title": {
              "maxLength": 100,
              "minLength": 3,
              "type": "string"
            }
          },
          "required": [
            "title",
            "description",
            "status",
            "evidenceEventIds"
          ],
          "type": "object"
        },
        "practice": {
          "description": "Drills for going further on this exact ability, each phrased as the learner's own goal because each one starts a session. Vary the transfer context, not just the difficulty.",
          "items": {
            "maxLength": 180,
            "minLength": 10,
            "type": "string"
          },
          "maxItems": 4,
          "type": "array"
        },
        "status": {
          "description": "Omit to let the evidence count decide. Set it only to say something the count cannot, such as marking a long-untouched ability stale.",
          "enum": [
            "uncertain",
            "developing",
            "independent",
            "stale"
          ],
          "type": "string"
        },
        "summary": {
          "description": "One sentence in plain words: what the learner can now do. Written to them, not about them.",
          "maxLength": 220,
          "minLength": 10,
          "type": "string"
        },
        "title": {
          "maxLength": 120,
          "minLength": 2,
          "type": "string"
        }
      },
      "required": [
        "title",
        "markdown",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "visualize_explain": {
    "description": "Direct the animation the learner sees. Pick the two to five steps that carry the explanation; for each, write one plain sentence, name what to draw, and say how long to hold on it. It plays inline in your reply at the pace you set, and they can pause and step through it. Afterwards write the one thing it is evidence for — do not describe the frames again in prose.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "autoplay": {
          "description": "Defaults to true for more than one step. Set false when the sequence is meant to be read rather than watched.",
          "type": "boolean"
        },
        "runId": {
          "minLength": 1,
          "type": "string"
        },
        "steps": {
          "items": {
            "additionalProperties": false,
            "properties": {
              "caption": {
                "description": "What to notice in this step, addressed to the learner. One sentence, no preamble.",
                "maxLength": 200,
                "minLength": 3,
                "type": "string"
              },
              "focus": {
                "description": "Draw only these: local names like \"counts\", or heap ids like \"@n2\". Use it — a picture of the two things your sentence is about lands, and a picture of every object in scope does not. Omit only when the whole state genuinely is the point.",
                "items": {
                  "type": "string"
                },
                "maxItems": 6,
                "type": "array"
              },
              "hold": {
                "description": "Seconds to stay on this step while it plays. Give the step where the thing goes wrong longer than the ones setting it up. Defaults to the caption's reading time.",
                "maximum": 6,
                "minimum": 0.6,
                "type": "number"
              },
              "step": {
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "step",
              "caption"
            ],
            "type": "object"
          },
          "maxItems": 8,
          "minItems": 1,
          "type": "array"
        },
        "takeaway": {
          "description": "Optional: the one sentence the whole sequence adds up to, shown under it.",
          "maxLength": 400,
          "type": "string"
        },
        "title": {
          "description": "What this shows, in the learner's language. Not the tool's name for it.",
          "maxLength": 90,
          "minLength": 3,
          "type": "string"
        }
      },
      "required": [
        "runId",
        "title",
        "steps",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "visualize_find": {
    "description": "Find the step that matters in a traced run. Every filter given must hold. Use this instead of guessing a step index or walking steps from zero — the interesting step in a 400-step run is never step 3.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "branch": {
          "description": "Steps whose branch test evaluated this way — false is how you find where a loop stopped.",
          "type": "boolean"
        },
        "event": {
          "description": "Steps of this kind.",
          "enum": [
            "call",
            "step",
            "return",
            "exception",
            "condition"
          ],
          "type": "string"
        },
        "limit": {
          "maximum": 24,
          "minimum": 1,
          "type": "integer"
        },
        "line": {
          "description": "Steps executing this source line.",
          "type": "integer"
        },
        "runId": {
          "description": "From visualize_run.",
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "Steps where some local reads as this, matched as a substring of the formatted value.",
          "type": "string"
        },
        "variable": {
          "description": "Steps where this local changed value, including where it first appeared.",
          "type": "string"
        }
      },
      "required": [
        "runId",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "visualize_read_step": {
    "description": "Read one instant of a traced run exactly: every local in scope, the objects those locals point at, the branch decision if there was one, and what changed since the step before. Read the steps that carry the explanation, not the run — visualize_find names them, and a digest plus two or three read steps is a finished answer.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "runId": {
          "minLength": 1,
          "type": "string"
        },
        "step": {
          "description": "The step index, from visualize_find or the digest.",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "runId",
        "step",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "visualize_run": {
    "description": "Trace one run. Either the learner's own file (from: \"attempt\") to explain why THEIR code does what it does, or a short snippet you write yourself (`code`) to illustrate an idea that is not in their file — a two-line snippet traced and drawn is a good answer to \"why does this give 4 and not 5\". Returns a compact digest of the run: which lines ran and how often, which way every branch went, what each variable started and ended as. Never trace a working solution to the challenge the learner is currently on.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "code": {
          "description": "The source to trace, when from is code. Keep it to the smallest program that shows the idea.",
          "maxLength": 4000,
          "type": "string"
        },
        "from": {
          "default": "code",
          "description": "attempt reads the learner's own implementation file in this session; code traces what you pass below.",
          "enum": [
            "attempt",
            "code"
          ],
          "type": "string"
        },
        "maxSteps": {
          "description": "Step budget. The default is ample; raise it only for a run you know is long.",
          "maximum": 6000,
          "minimum": 1,
          "type": "integer"
        },
        "setup": {
          "description": "The single call to trace, e.g. `search([1,3,9], 9)`. Without this nothing runs.",
          "maxLength": 400,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "setup",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "web_fetch": {
    "description": "Read one or more web pages in full, by URL. Use it after web_search has told you which page is worth reading. http and https only.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "urls": {
          "items": {
            "format": "uri",
            "type": "string"
          },
          "maxItems": 5,
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "urls",
        "actionTitle"
      ],
      "type": "object"
    }
  },
  "web_search": {
    "description": "Search the web for current, external information: what a company's interviews actually cover, what a library's current API is, what a topic's standard formulation is. Returns titles, URLs, and short extracts. Use it to ground a goal in reality when the learner's own record cannot answer the question, and prefer one focused query over several vague ones.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "additionalProperties": false,
      "properties": {
        "actionTitle": {
          "description": "A short, specific title for this step, in the learner's language, shown as the row for this call in the transcript. Say what this particular call is for — \"Checking whether arrays have been tested\", \"Reading how you solved the window repair\" — not the tool's generic purpose. Sentence case, no trailing period, present participle while it runs.",
          "maxLength": 70,
          "minLength": 3,
          "type": "string"
        },
        "limit": {
          "default": 5,
          "maximum": 10,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "minLength": 2,
          "type": "string"
        }
      },
      "required": [
        "query",
        "actionTitle"
      ],
      "type": "object"
    }
  }
}
